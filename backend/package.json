{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "start": "node index.js",
    "build": "rm -rf public && cd ../frontend && npm run build && cp -r dist ../backend/public"
  },
  "dependencies": {
    "argon2": "^0.41.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-validator": "^7.2.1",
    "jsonwebtoken": "^9.0.2",
    "mysql-error-keys": "^0.1.3",
    "mysql2": "^3.12.0",
    "node-ipinfo": "^4.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
